<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="BevasarloLista.Views.LoginPage"
             x:Uid="bejelentkezes"
             Title="{Binding Title}"
             >

    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand">
            <Button x:Name="loginFBButton"  MinimumHeightRequest="30"
             Command="{Binding LoginFacebookCommand}"/>
            <Button x:Name="loginGoogleButton"  MinimumHeightRequest="30"
             Command="{Binding LoginGoogleCommand}"/>
        </StackLayout>
    </ContentPage.Content>
  
</ContentPage>